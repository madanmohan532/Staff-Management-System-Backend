package training.iqgateway.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class TestController {

	// This is a simple test endpoint to verify that the controller is set up correctly.
	// You can access it at /api/test
	@GetMapping("/api/test")
	public String test() {
		return "Test endpoint is working!";
	}
	
	
	
}
